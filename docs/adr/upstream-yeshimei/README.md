# 上游 ADR 归档（yeshimei/bz）

本目录是 **只读归档**：GitHub `yeshimei/bz` 线上独有的 ADR，规则见本地 ADR-0119。

- 文件名保留上游原名——**包括重号**（如两份 `0110-*`）与**不规范命名**（如 `ADR-0124-shared-lock-screen.md`），不重编、不改名、不合并。
- 本目录内容**不修改**。对上游决策的评估、反驳、吸收与否，写在本地 `docs/adr/` 里。
- 与本地 ADR 的关系：本地编号连续发放（`0117` 起），两边同号不同题属预期；正文引用上游一律写「上游 ADR-XXXX」，裸写「ADR-XXXX」默认指本地。
- 双边**同名同内容**的 ADR（共同祖先期产物）不在此目录，仍平铺在 `docs/adr/`。

数据来源：上游 tip `7b06f4b4`（2026-09-12 22:24）；比对基线 `.scratch/upstream-compare/report/06-docs.json`。
