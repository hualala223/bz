# 292: 门面正名「娱乐」+ 数据目录迁移 + 「感想」文案

**What to build:** 用户打开面板看到的一切都叫「娱乐」——面板标题、命令显示名、提示文案；插件启动时旧 `我的/影视` 目录整体搬到 `我的/娱乐`（Obsidian renameFile 保持双链），设置里存的是旧默认值时平移到新默认值；详情页与表单里的「影评」字样全部改称「感想」（frontmatter 键「影评」不动，零数据迁移）。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 面板标题/命令显示名/提示文案均为「娱乐」，无「影院/影视」残留（历史注释除外）
- [ ] 启动时旧目录存在且新目录不存在 → 整目录迁移；新目录已存在 → 不动
- [ ] `cinemaFolderPath` 为旧默认值时平移新默认值；用户自定义值不动
- [ ] UI「影评」→「感想」，fm 键「影评」读写不变
- [ ] 数据层测试：默认目录回落新值；迁移两分支断言
